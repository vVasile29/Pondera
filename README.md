# MetricMatch

A decision engine for when there are too many variables to keep in your head.

Define activities, set metric weights, score candidates, and discover what really matters.

Built with FastAPI + SQLAlchemy + Jinja2 + Alpine.js + Chart.js.
